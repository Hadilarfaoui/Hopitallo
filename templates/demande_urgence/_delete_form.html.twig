<form method="post" action="{{ path('app_demande_urgence_delete', {'id': demande_urgence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_urgence.id) }}">
    <button class="btn">Delete</button>
</form>
