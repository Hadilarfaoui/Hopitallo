{% extends 'base_Medecin.html.twig' %}

{% block title %}Rdv{% endblock %}

{% block body %}
    <h1>Rdv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rdv.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ rdv.titre }}</td>
            </tr>
            <tr>
                <th>Intervele_time</th>
                <td>{{ rdv.interveleTime }}</td>
            </tr>
            <tr>
                <th>Date_rdv</th>
                <td>{{ rdv.dateRdv ? rdv.dateRdv|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ rdv.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rdv_index') }}">back to list</a>

    <a href="{{ path('app_rdv_edit', {'id': rdv.id}) }}">edit</a>

    {{ include('rdv/_delete_form.html.twig') }}
{% endblock %}
